type Device = {
    id: string;
    deviceName: string;
    devicePort: string;
    ipAddress: string;
    mode: string;
    samplingInterval: string;
    timeout: string;
    deviceProfileId: number;
  }